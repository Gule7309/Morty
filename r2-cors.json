{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:5173",
          "https://REPLACE_WITH_FRONTEND_DOMAIN"
        ],
        "methods": ["GET", "PUT"],
        "headers": ["Content-Type", "If-None-Match"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
